import React from 'react';
import styled from 'styled-components';

const ContainerHeader = (): JSX.Element => {
  return <ContainerHeaderStyle>ContainerHeader</ContainerHeaderStyle>;
};

export default ContainerHeader;

const ContainerHeaderStyle = styled.div``;
