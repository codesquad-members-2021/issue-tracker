import React, { ReactElement } from 'react';
import styled from 'styled-components';

interface Props {}

export default function UserSelectItem({}: Props): ReactElement {
  return <UserSelectItemBlock></UserSelectItemBlock>;
}

const UserSelectItemBlock = styled.div``;
