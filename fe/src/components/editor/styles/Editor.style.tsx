import styled from "styled-components";

export const List = styled.ul`
  all: unset;
  display: flex;
  cursor: pointer;
`;

export const ListItem = styled.li`
  list-style: none;
  margin-right: 2em;
`;
