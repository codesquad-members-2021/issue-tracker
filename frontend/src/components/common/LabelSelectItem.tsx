import React, { ReactElement } from 'react';
import styled from 'styled-components';

interface Props {}

export default function LabelSelectItem({}: Props): ReactElement {
  return <LabelSelectItemBlock></LabelSelectItemBlock>;
}

const LabelSelectItemBlock = styled.div``;
