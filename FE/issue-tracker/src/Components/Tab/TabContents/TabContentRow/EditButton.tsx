import { TabAssets as Icon, LabelMilestoneTable as S } from "../../TabStyles";

const EditButton = () => {
  return (
    <S.TableButtonDiv>
      <Icon.EditIcon />
      편집
    </S.TableButtonDiv>
  );
};

export default EditButton;
