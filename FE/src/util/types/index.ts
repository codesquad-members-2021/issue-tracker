import { TChildren, TMouseEvent } from './normal';
import {
  ILabel, ILabelsInfo,
  IMilestone, IMilestonesInfo,
  IUser, IUsersInfo,
  IIssue, IIssuesInfo,
  IAllGetRequestDatas,
} from './api';

export type {
  // 1) 일반
  TChildren,
  TMouseEvent,
  // 2) API 관련
  ILabel, ILabelsInfo,
  IMilestone, IMilestonesInfo,
  IUser, IUsersInfo,
  IIssue, IIssuesInfo,
  IAllGetRequestDatas,
};
