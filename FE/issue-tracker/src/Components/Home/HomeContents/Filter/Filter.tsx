import { FilterSearchBar as S, HomeAssets as SVG } from "../../HomeStyles";

const Filter = () => {
  return (
    <S.FilterDiv>
      필터
      <SVG.Down />
    </S.FilterDiv>
  );
};

export default Filter;
